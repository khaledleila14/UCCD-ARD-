#include <stdio.h>
void main ()
{
	int x=0 , y=0 , sum=0 ;
	printf ("enter the first num");
	scanf (" %d",&x);
	printf ("enter the second num");
	scanf (" %d",&y);
	while (sum = x + y)
	{
		printf("the sum = %d\n",sum);
	printf ("enter the first num");
	scanf (" %d",&x);
	printf ("enter the second num");
	scanf (" %d",&y);
	}
	}